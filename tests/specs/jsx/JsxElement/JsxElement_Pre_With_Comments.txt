-- file.tsx --
~~ deno: true ~~
== should not duplicate jsx empty expression block comment inside pre ==
function ErrorPage500(props: ErrorPageProps) {
  return (
    <pre>
      {props.url}
      {/* {props.url} */}
    </pre>
  );
}

[expect]
function ErrorPage500(props: ErrorPageProps) {
  return (
    <pre>
      {props.url}
      {/* {props.url} */}
    </pre>
  );
}

== should not duplicate adjacent jsx empty expression comments inside pre ==
const t = (
  <pre>
    {/* a */}
    {/* b */}
  </pre>
);

[expect]
const t = (
  <pre>
    {/* a */}
    {/* b */}
  </pre>
);
