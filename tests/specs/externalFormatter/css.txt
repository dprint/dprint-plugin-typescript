-- file.js --
== should format CSS tagged template with a single quasi ==
import styled, { css } from 'styled-components'

const Header = css`
height: 40px;  padding: 0 15px;
display: flex;          align-items: center;
border-top-left-radius: 5px;
border-top-right-radius: 5px;
`;

function foo() {
    const Tag = css`height: 40px;  padding: 0 15px;`
}

const Foo = styled.div`padding: 1px;`;

[expect]
import styled, { css } from "styled-components";

const Header = css`
    height: 40px;
    padding: 0 15px;
    display: flex;
    align-items: center;
    border-top-left-radius: 5px;
    border-top-right-radius: 5px;
`;

function foo() {
    const Tag = css`
        height: 40px;
        padding: 0 15px;
    `;
}

const Foo = styled.div`
    padding: 1px;
`;
