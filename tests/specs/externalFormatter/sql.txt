-- file.js --
== should format SQL tagged template ==
let query = sql`SELECT   *    FROM users;`;

query = sql`SELECT   ${table}.${field}    FROM  ${table};`;

[expect]
let query = sql`
    SELECT
        *
    FROM
        users;
`;

query = sql`
    SELECT
        ${table}.${field}
    FROM
        ${table};
`;
