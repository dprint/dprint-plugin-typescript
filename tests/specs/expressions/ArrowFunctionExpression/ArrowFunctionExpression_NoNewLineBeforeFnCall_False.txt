~~ arrowFunction.noNewLineBeforeFnCall: false ~~
== should insert new line after arrow when option is false ==
[].map((it) => test({
    foo: 1,
    bar: 2
}));

[expect]
[].map((it) =>
    test({
        foo: 1,
        bar: 2,
    })
);
