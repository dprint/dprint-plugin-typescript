~~ arrowFunction.noNewLineBeforeFnCall: true ~~
== should not insert new line after arrow when option is true ==
[].map((it) => test({
    foo: 1,
    bar: 2
}));

[expect]
[].map((it) => test({
    foo: 1,
    bar: 2,
}));
